{
  "name": "server",
  "types": "./src/index.ts",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun run --hot --port=3000 src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.0",
    "@hono/zod-openapi": "^0.18.3",
    "hono": "^4.6.16",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^3.0.2"
  }
}